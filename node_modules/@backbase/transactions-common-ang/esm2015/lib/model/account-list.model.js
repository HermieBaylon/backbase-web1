export function fromArrangementsResponse(response) {
    return (response.body || []).map((item) => ({
        id: item.id,
        name: item.name,
        displayName: item.displayName,
        alias: item.bankAlias,
        IBAN: item.IBAN,
        BBAN: item.BBAN,
        currency: item.currency,
    }));
}
//# sourceMappingURL=account-list.model.js.map