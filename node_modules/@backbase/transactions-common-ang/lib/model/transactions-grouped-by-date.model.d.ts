import { Transaction } from './transaction.model';
import { TransactionsList } from './transactions-list.model';
export interface TransactionsGroupedByDate {
    [date: string]: Transaction[];
}
export declare namespace TransactionsGroupedByDate {
    function groupByDate(transactionsList: TransactionsList | undefined): TransactionsGroupedByDate;
}
