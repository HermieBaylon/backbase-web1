import { HttpResponse } from '@angular/common/http';
import { Category } from '@backbase/data-ang/categories-management';
import { Customizable } from './customizable.model';
export interface CategoryItem extends Customizable {
    categoryId: number;
    categoryName: string;
}
export declare function categoriesManagementMap(item: Category): CategoryItem;
export declare function fromCategoriesManagementResponse(response: HttpResponse<Array<Category>>): CategoryItem[];
