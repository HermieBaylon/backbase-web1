export const getRelativeDate = (next) => {
    const date = new Date();
    if (next) {
        date.setDate(date.getDate() + next);
    }
    return date.toISOString().slice(0, 10);
};
//# sourceMappingURL=date.js.map