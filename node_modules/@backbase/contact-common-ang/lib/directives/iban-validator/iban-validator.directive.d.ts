import { AbstractControl, Validator } from '@angular/forms';
import { IBANCountryList } from './countrySpec';
import * as i0 from "@angular/core";
/**
 * This is a directive to validate iban
 *
 * @usageNotes
 *
 * ### validates the iban
 *
 * ```html
 * <input type="text" #ibanControl="ngModel" [(ngModel)]="iban" bbIbanValidatorUi />
 * ```
 * @ngModule IbanValidatorModule
 */
export declare class IbanValidatorDirective implements Validator {
    private readonly ibanCountryList;
    constructor(ibanCountryList: IBANCountryList);
    validate(control: AbstractControl): {
        [key: string]: any;
    } | null;
    static ɵfac: i0.ɵɵFactoryDeclaration<IbanValidatorDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<IbanValidatorDirective, "[bbIbanValidator]", never, {}, {}, never>;
}
