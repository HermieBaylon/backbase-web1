export declare type StopCheckStatus = 'PENDING' | 'ACCEPTED' | 'REJECTED' | 'EXPIRED';
export declare const StopCheckStatus: {
    PENDING: StopCheckStatus;
    ACCEPTED: StopCheckStatus;
    REJECTED: StopCheckStatus;
    EXPIRED: StopCheckStatus;
};
