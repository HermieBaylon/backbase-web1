import { Route } from '@angular/router';
import '@angular/localize/init';
export declare const mailboxTypeTitles: {
    inbox: string;
    outbox: string;
    drafts: string;
};
export declare const defaultRoute: Route;
