import { ActivatedRoute, Router } from '@angular/router';
import { Observable } from 'rxjs';
export declare const getRouteParams: (route: ActivatedRoute, param: string, router?: Router | undefined) => Observable<string | null>;
