export function scanAndMergePaginatedItems() {
    return (memo, item) => {
        if (item.from === 0 || item.paginationType === 'pagination') {
            return item;
        }
        return Object.assign(Object.assign({}, item), { items: [...memo.items, ...item.items] });
    };
}
export const extractDataFromResponse = (from, paginationType) => {
    return (response) => ({
        items: response.body || [],
        totalItems: parseInt(response.headers.get('x-total-count') || `${(response.body || []).length}`, 10),
        from,
        paginationType,
    });
};
//# sourceMappingURL=pagination-helpers.js.map