import { PaymentFormField, PaymentFormFieldOptions, PaymentFormFieldConfig } from '@backbase/payment-orders-ang';
import { FormGroup, AbstractControl } from '@angular/forms';
import { VisibilityService } from '../services/visibility.service';
export declare class ActivatableFormField implements PaymentFormField {
    private readonly service;
    /**
     * Payment form field options
     */
    options: PaymentFormFieldOptions;
    /**
     * Payment form field configuration
     */
    config: PaymentFormFieldConfig;
    /**
     * Form group object
     * Tracks the value and validity state of a group of FormControl instances
     */
    group: FormGroup;
    /**
     * Base class for FormControl, FormGroup, and FormArray
     */
    control: AbstractControl;
    /**
     * @internal
     */
    constructor(service: VisibilityService);
    /**
     * @internal
     */
    private showDependantFields;
    /**
     * @internal
     */
    private hideDependantFields;
    /**
     * Tracks if a form item should be shown or not
     */
    toggleDependants(): void;
}
