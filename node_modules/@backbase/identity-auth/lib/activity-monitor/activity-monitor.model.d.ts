import { InjectionToken } from '@angular/core';
export declare const ACTIVITY_MONITOR_CONFIG: InjectionToken<Partial<ActivityMonitorConfig>>;
export interface ActivityMonitorConfig {
    /**
     * Duration of user inactivity that ends the countdown.
     * Default value is 180 seconds.
     */
    maxInactivityDuration: number;
    /**
     * Duration of countdown.
     * Default value is 30 seconds.
     */
    countdownDuration: number;
}
export declare type ActivityEvent = {
    type: 'start' | 'end' | 'reset';
} | {
    type: 'tick';
    remaining: number;
};
