import { ProductKind } from '@backbase/product-summary-common-ang';
export interface ProductKindCustomized extends ProductKind {
    id: string;
    name?: string;
    aggregatedBalance?: string;
    currency?: string;
}
