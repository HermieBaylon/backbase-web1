import { PaymentFormGroup } from '@backbase/payment-orders-ang';
export declare const BaseFields: PaymentFormGroup[];
export declare const PaymentTypes: {
    ILE_POSTFIX: string;
    CLOSED_POSTFIX: string;
    SEPA_PREFIX: string;
    SEPA: string;
};
