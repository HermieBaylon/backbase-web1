import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { ProductsExternalInfoService } from '@backbase/product-summary-common-ang';
import { Pubsub } from '@backbase/foundation-ang/web-sdk';
import { ArrangementsService } from '../arrangements.service';
import { ProductSummaryListBaseComponent } from '../product-summary-list-base/product-summary-list-base.component';
import { BbTemplate } from '@backbase/foundation-ang/core';
import { ProductKinds } from '../product-summary-base.service';
import * as i0 from "@angular/core";
/**
 * A widget for displaying a list of accounts
 *
 * @usageNotes
 *
 * ### Include this widget in your `AppModule`:
 *
 * ```typescript
 * @NgModule({
 *   imports: [
 *     ArrangementsListWidgetComponent,
 *   ],
 *   ...
 * })
 * export class AppModule {}
 * ```
 *
 * ### Include this widget (standalone model)
 *
 * ```typescript
 * export const environment: Environment = {
 *   ...
 *   bootstrap: {
 *     pageModel: {
 *       name: 'root',
 *       properties: {},
 *       children: [
 *         {
 *           name: 'product-summary-account-details-widget-ang',
 *           properties: {
 *            classId: 'ArrangementsListWidgetComponent',
 *            showIcons: true,
 *           },
 *         },
 *       ],
 *     },
 *   },
 * };
 * ```
 */
export declare class ArrangementsListWidgetComponent extends ProductSummaryListBaseComponent implements OnInit, OnDestroy {
    protected readonly service: ArrangementsService;
    protected readonly productsExternalInfoService: ProductsExternalInfoService;
    protected readonly eventBusService: Pubsub;
    /**
     * Input for setting `viewName` property
     */
    viewName?: string;
    /**
     * Emits arrangement id when pay now is clicked
     */
    payNowClick: EventEmitter<string>;
    /**
     * Constructor
     *
     * @param service
     * @param productsExternalInfoService
     * @param eventBusService
     */
    constructor(service: ArrangementsService, productsExternalInfoService: ProductsExternalInfoService, eventBusService: Pubsub);
    ngOnInit(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ArrangementsListWidgetComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ArrangementsListWidgetComponent, "bb-arrangements-list-widget", never, { "viewName": "viewName"; }, { "payNowClick": "payNowClick"; }, never, never>;
}
/**
 * This is an extension slot to be used for customizing items with the `overdueAccounts` productKind
 *
 * @usageNotes
 *
 * ### Customize `Needs Your Attention`
 *
 * ```html
 * <ng-template
 *   bbProductSummaryListItemNeedsAttentionAccountsCustomizable
 *   let-hostRef
 *   let-productKind="context">
 *   Your custom template here
 * </ng-template>
 * ```
 *
 * @NgModule ProductSummaryListWidgetModule
 */
export declare class ProductSummaryListItemNeedsAttentionAccountsCustomizableDirective extends BbTemplate<ProductSummaryListBaseComponent, ProductKinds> {
    static ɵfac: i0.ɵɵFactoryDeclaration<ProductSummaryListItemNeedsAttentionAccountsCustomizableDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<ProductSummaryListItemNeedsAttentionAccountsCustomizableDirective, "[bbProductSummaryListItemNeedsAttentionAccountsCustomizable]", never, {}, {}, never>;
}
