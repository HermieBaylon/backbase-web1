import { Observable } from 'rxjs';
import { ProductSummaryHttpService } from '@backbase/arrangement-manager-http-ang';
import { AdditionalProperties, Product } from '@backbase/product-summary-common-ang';
import { ProductKinds, ProductSummaryBaseService } from './product-summary-base.service';
import * as i0 from "@angular/core";
export interface TotalBalance extends AdditionalProperties {
    aggregatedBalance: string;
    currency: string;
}
export interface ProductKind extends AdditionalProperties {
    id: string;
    name?: string;
    aggregatedBalance?: string;
    currency?: string;
    products?: Array<Product>;
}
/**
 * Service for fetching and storing product kinds
 *
 * This service relies on providers from `ProductSummaryListWidgetModule`.
 *
 * @see ProductSummaryListWidgetModule
 *
 * @usageNotes
 *
 * ### Ensure the ProductSummaryListWidgetModule is imported to your component module
 *
 * ```ts
 * @NgModule({
 *   ...
 *   imports: [
 *     ...
 *     ProductSummaryListWidgetModule,
 *   ],
 * })
 * export class MyWidgetModule {}
 * ```
 *
 * ### Inject this service into your component
 *
 * ```ts
 * @Component({
 *   ...
 *   providers: [ProductSummaryService],
 * })
 *  export class MyComponent {
 * ```
 */
export declare class ProductSummaryService extends ProductSummaryBaseService {
    private readonly productSummaryDataService;
    constructor(productSummaryDataService: ProductSummaryHttpService);
    readonly productKinds: Observable<ProductKinds | undefined>;
    static ɵfac: i0.ɵɵFactoryDeclaration<ProductSummaryService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ProductSummaryService>;
}
