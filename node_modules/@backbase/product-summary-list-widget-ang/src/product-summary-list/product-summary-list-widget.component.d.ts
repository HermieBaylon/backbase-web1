import { OnDestroy } from '@angular/core';
import { ProductSummaryService } from '../product-summary.service';
import { ProductsExternalInfoService } from '@backbase/product-summary-common-ang';
import { Pubsub } from '@backbase/foundation-ang/web-sdk';
import { ProductSummaryListBaseComponent } from '../product-summary-list-base/product-summary-list-base.component';
import * as i0 from "@angular/core";
/**
 * A widget for displaying a list of accounts
 *
 * @usageNotes
 *
 * ### Include this widget in your `AppModule`:
 *
 * ```typescript
 * @NgModule({
 *   imports: [
 *     ProductSummaryListWidgetModule,
 *   ],
 *   ...
 * })
 * export class AppModule {}
 * ```
 *
 * ### Include this widget (standalone model)
 *
 * ```typescript
 * export const environment: Environment = {
 *   ...
 *   bootstrap: {
 *     pageModel: {
 *       name: 'root',
 *       properties: {},
 *       children: [
 *         {
 *           name: 'product-summary-account-details-widget-ang',
 *           properties: {
 *            classId: 'ProductSummaryListWidgetComponent',
 *            showIcons: true,
 *           },
 *         },
 *       ],
 *     },
 *   },
 * };
 * ```
 */
export declare class ProductSummaryListWidgetComponent extends ProductSummaryListBaseComponent implements OnDestroy {
    protected readonly service: ProductSummaryService;
    protected readonly productsExternalInfoService: ProductsExternalInfoService;
    protected readonly eventBusService: Pubsub;
    /**
     * Constructor
     *
     * @param service
     * @param productsExternalInfoService
     * @param eventBusService
     */
    constructor(service: ProductSummaryService, productsExternalInfoService: ProductsExternalInfoService, eventBusService: Pubsub);
    static ɵfac: i0.ɵɵFactoryDeclaration<ProductSummaryListWidgetComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ProductSummaryListWidgetComponent, "bb-product-summary-list-widget", never, {}, {}, never, never>;
}
