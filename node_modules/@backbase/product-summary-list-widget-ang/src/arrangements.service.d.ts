import { Observable } from 'rxjs';
import { ArrangementViewsHttpService } from '@backbase/arrangement-manager-http-ang';
import { ProductKinds, ProductSummaryBaseService } from './product-summary-base.service';
import * as i0 from "@angular/core";
/**
 * Service for fetching and storing product kinds
 *
 * This service relies on providers from `ProductSummaryListWidgetModule`.
 *
 * @see ProductSummaryListWidgetModule
 *
 * @usageNotes
 *
 * ### Ensure the ProductSummaryListWidgetModule is imported to your component module
 *
 * ```ts
 * @NgModule({
 *   ...
 *   imports: [
 *     ...
 *     ProductSummaryListWidgetModule,
 *   ],
 * })
 * export class MyWidgetModule {}
 * ```
 *
 * ### Inject this service into your component
 *
 * ```ts
 * @Component({
 *   ...
 *   providers: [ProductSummaryService],
 * })
 *  export class MyComponent {
 * ```
 */
export declare class ArrangementsService extends ProductSummaryBaseService {
    private readonly arrangementViewsHttpService;
    private readonly viewNameSubject;
    constructor(arrangementViewsHttpService: ArrangementViewsHttpService);
    readonly productKinds: Observable<ProductKinds | undefined>;
    setViewName(viewName: string): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ArrangementsService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ArrangementsService>;
}
