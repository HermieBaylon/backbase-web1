import { Observable } from 'rxjs';
export declare const getPropertyValue: <T>(input: Observable<T | undefined>, prop: Observable<T>) => Observable<T>;
export declare const defaultModelObservable: <T>(value: T) => Observable<T>;
