import { Observable } from 'rxjs';
export interface Attachment {
    id: string;
    filename: string;
    size: number;
    createdAt: string;
    link?: string;
    linkStream?: Observable<ArrayBuffer>;
}
