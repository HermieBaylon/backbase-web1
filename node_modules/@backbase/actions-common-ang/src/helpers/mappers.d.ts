import { ActionParent, ActionRecipeSpecification, BatchRecipe, Action, Recurrence } from '@backbase/data-ang/actions';
import { SpecificationsWithAction } from '../models/specifications-with-action.model';
import { ActionRecipeFormItemValue } from '../models/action-recipes-form-value.model';
import { ActionRecipeItem } from '../models/action-recipe-item.model';
export declare const fromHttpResponse: <T>(body: T | null) => T;
export declare const secondsToMilliseconds: (seconds: number) => number;
export declare const actionRecipeFormItemToRequest: ({ id, name, amount, actions, recurrence, ...restActionRecipe }: ActionRecipeFormItemValue) => BatchRecipe;
export declare const parseActionRecipeFormItem: ({ id, name, amount, actions, recurrence, ...restActionRecipe }: ActionRecipeFormItemValue) => ActionRecipeItem;
export declare const arrangementActionRecipeToRequest: (arrangementId: string) => (actionRecipeFormItemValue: ActionRecipeFormItemValue) => BatchRecipe;
export declare const mergeNotificationChannels: (channels: ActionParent[], defaultChannels: ActionParent[]) => Action[];
export declare const mergeSpecificationsWithActionRecipes: (specs: ActionRecipeSpecification[], recipes: ActionRecipeItem[]) => SpecificationsWithAction;
export declare const getMappedRecurrenceResponse: (responseRecurrence: Recurrence, { recurrence: cachedRecurrence }: ActionRecipeFormItemValue) => Recurrence;
