import { EventEmitter, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { ActionRecipeGetResponseBody, ActionRecipeSpecification, SpecificationAction } from '@backbase/data-ang/actions';
import { ActionsFormBuilderService } from '../services/actions-form-builder.service';
import { AccountArrangementItem } from '@backbase/data-ang/arrangements';
import { ActionRecipeFormItemValue } from './action-recipes-form-value.model';
import * as i0 from "@angular/core";
export declare abstract class ArrangementBasedRecipeModel implements OnInit, OnChanges, OnDestroy {
    private readonly actionsFormBuilderService;
    /**
     * Account data.
     */
    account?: AccountArrangementItem;
    /**
     * Action recipe data.
     */
    actionRecipe?: ActionRecipeGetResponseBody;
    /**
     * Specification setter.
     */
    set specification(specification: ActionRecipeSpecification);
    /**
     * Specification getter.
     */
    get specification(): ActionRecipeSpecification;
    /**
     * Specification value.
     */
    specificationValue?: ActionRecipeSpecification;
    /**
     * On submit event emitter.
     */
    submit: EventEmitter<ActionRecipeFormItemValue>;
    /**
     * Specifications with actions.
     */
    specificationActions: SpecificationAction[];
    /**
     * Account data.
     */
    formGroup?: FormGroup;
    /**
     * Loading flag.
     */
    isLoading: boolean;
    private readonly destroy$;
    /**
     * Method to check if card form control is expanded.
     *
     * @returns boolean value
     */
    get isCardExpanded(): boolean;
    protected constructor(actionsFormBuilderService: ActionsFormBuilderService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    /**
     * Method to submit action recipe form.
     *
     * @param formValue - action recipe form value
     */
    onSubmit(formValue: ActionRecipeFormItemValue): void;
    /**
     * Method to set loading flag to true.
     */
    onActiveLoadingChange(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ArrangementBasedRecipeModel, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<ArrangementBasedRecipeModel, never, never, { "account": "account"; "actionRecipe": "actionRecipe"; "specification": "specification"; }, { "submit": "submit"; }, never>;
}
