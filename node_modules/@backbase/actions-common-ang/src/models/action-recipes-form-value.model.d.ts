import { DaysOfWeek } from '@backbase/actions-http-ang';
export interface ActionRecipeFormItemValue {
    id: string | null;
    name: string | null;
    active: boolean;
    specificationId: string;
    actions: ActionRecipeChannelsFormValue;
    amount: {
        amount: string;
        currency: any;
    };
    recurrence?: ActionRecurrenceFormValue;
}
export interface ActionRecipeChannelsFormValue {
    [key: string]: boolean;
}
export interface ActionRecurrenceFormValue {
    scheduleTime: number[];
    daysOfWeek?: DaysOfWeek[];
}
