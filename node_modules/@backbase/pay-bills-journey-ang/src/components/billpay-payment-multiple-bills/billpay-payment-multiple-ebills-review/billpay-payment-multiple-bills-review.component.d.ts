import { ChangeDetectorRef, ElementRef, OnDestroy } from '@angular/core';
import { BillpayDiscardChangesModalService, BillpayPaymentDataService, PaymentFormState } from '@backbase/billpay-journeys-common';
import { PayBillsNavigationService } from '../../../services/pay-bills-navigation.service';
import { Observable } from 'rxjs';
import { ActivatedRoute } from '@angular/router';
import { PayBillsStateService } from '../../../services/pay-bills-state.service';
import { PaymentResult } from '../../../model/payment.model';
import * as i0 from "@angular/core";
export declare class BillpayPaymentMultipleBillsReviewComponent implements OnDestroy {
    private readonly paymentStateService;
    private readonly dataService;
    private readonly navigationService;
    private readonly route;
    private readonly discardModalService;
    private cdRef;
    private readonly destroy$;
    private editMode;
    /**
     * Flag for payment error
     */
    paymentError: boolean;
    /**
     * Flag for submitting payment
     */
    submittingPayment: boolean;
    readonly id: string;
    /**
     * Refrence for fail alert
     */
    failAlert: ElementRef;
    /**
     * List of payments
     */
    payments$: Observable<PaymentFormState[] | undefined>;
    /**
     * Carry the amount for total amount to be paid
     */
    totalAmount$: Observable<number | undefined>;
    /**
     * List of responses results
     */
    results?: PaymentResult[];
    get isDiscardModalOpen(): boolean;
    constructor(paymentStateService: PayBillsStateService, dataService: BillpayPaymentDataService, navigationService: PayBillsNavigationService, route: ActivatedRoute, discardModalService: BillpayDiscardChangesModalService, cdRef: ChangeDetectorRef);
    ngOnDestroy(): void;
    onWindowUnload($event: BeforeUnloadEvent): void;
    /**
     * Methos to send payment to server
     */
    onPaymentConfirm(payments: PaymentFormState[]): void;
    private navigateToForm;
    private allFailed;
    openModal(): Observable<boolean>;
    onCancel(): void;
    onModalResolve(isConfirmed: boolean): void;
    onEdit(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<BillpayPaymentMultipleBillsReviewComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<BillpayPaymentMultipleBillsReviewComponent, "bb-billpay-payment-multiple-bills-review", never, {}, {}, never, never>;
}
