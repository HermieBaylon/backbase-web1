export interface ConversationStatus {
    ["conversationId"]: string;
    ["status"]: ConversationStatus.StatusEnum;
    ["additions"]?: {
        [key: string]: string;
    };
}
export declare namespace ConversationStatus {
    type StatusEnum = 'assigned' | 'ended' | 'resolved';
    const StatusEnum: {
        Assigned: StatusEnum;
        Ended: StatusEnum;
        Resolved: StatusEnum;
    };
}
