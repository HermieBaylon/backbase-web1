export interface ConversationStatusUpdate {
    ["status"]: ConversationStatusUpdate.StatusEnum;
    ["additions"]?: {
        [key: string]: string;
    };
}
export declare namespace ConversationStatusUpdate {
    type StatusEnum = 'assigned' | 'resolved' | 'ended';
    const StatusEnum: {
        Assigned: StatusEnum;
        Resolved: StatusEnum;
        Ended: StatusEnum;
    };
}
