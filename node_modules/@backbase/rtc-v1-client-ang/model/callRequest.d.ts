export interface CallRequest {
    ["type"]: CallRequest.TypeEnum;
    ["additions"]?: {
        [key: string]: string;
    };
}
export declare namespace CallRequest {
    type TypeEnum = 'voice' | 'video';
    const TypeEnum: {
        Voice: TypeEnum;
        Video: TypeEnum;
    };
}
