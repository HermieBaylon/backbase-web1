import { ChangeDetectorRef, OnDestroy, OnInit, ElementRef } from '@angular/core';
import { NgControl } from '@angular/forms';
import * as i0 from "@angular/core";
export declare class IbanDirective implements OnInit, OnDestroy {
    private readonly el;
    private readonly ngControl;
    private readonly changeDetectorRef;
    /**
     * The max length for the IBAN number, mostly 34
     */
    maxLen: number;
    private value;
    private readonly editPositionRestorer$;
    private readonly garbageCollector$;
    private readonly transformValue;
    constructor(el: ElementRef, ngControl: NgControl, changeDetectorRef: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<IbanDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<IbanDirective, "[bbIban]", never, { "maxLen": "maxLen"; }, {}, never>;
}
