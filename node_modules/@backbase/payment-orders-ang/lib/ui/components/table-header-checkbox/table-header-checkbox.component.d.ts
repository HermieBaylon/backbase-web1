import { ElementRef, OnInit, OnDestroy } from '@angular/core';
import { Subject } from 'rxjs';
import { TableDirective } from '@backbase/ui-ang/table';
import * as i0 from "@angular/core";
export declare class TableHeaderCheckboxComponent implements OnInit, OnDestroy {
    private readonly parentTable;
    checkboxInput?: ElementRef<HTMLInputElement>;
    /**
     * Input which sends current selection set to be cleared
     */
    clearSelection: Subject<Set<string>>;
    private getSelectedItemsCount;
    constructor(parentTable: TableDirective);
    get isChecked(): boolean;
    get isIntermediate(): boolean;
    onClick(event: Event): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<TableHeaderCheckboxComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<TableHeaderCheckboxComponent, "bb-table-header-checkbox", never, { "clearSelection": "clearSelection"; }, {}, never, never>;
}
