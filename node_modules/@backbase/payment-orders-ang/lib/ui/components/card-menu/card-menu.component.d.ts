import { EventEmitter, AfterViewInit } from '@angular/core';
import { NgbTooltipConfig } from '@ng-bootstrap/ng-bootstrap';
import * as i0 from "@angular/core";
interface CardMenuItem {
    name: string;
    description?: string;
}
export declare class PayordCardMenuComponent implements AfterViewInit {
    /**
     * List of tab items.
     * Default is empty list.
     */
    items: Array<CardMenuItem>;
    /**
     * Initial selected tab item.
     * Default is `0`.
     */
    initialSelection: number;
    /**
     * Select event emitted when a new tab item has been selected.
     */
    select: EventEmitter<CardMenuItem>;
    activeItem: number;
    constructor(config: NgbTooltipConfig);
    ngAfterViewInit(): void;
    onSelect(index: number): void;
    trackByName(index: number, item: CardMenuItem): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<PayordCardMenuComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<PayordCardMenuComponent, "bb-payord-card-menu", never, { "items": "items"; "initialSelection": "initialSelection"; }, { "select": "select"; }, never, never>;
}
export {};
