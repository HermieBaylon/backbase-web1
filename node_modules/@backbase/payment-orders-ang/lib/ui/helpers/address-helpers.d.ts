import { AccountInformation } from '@backbase/data-ang/contact-manager';
import { Address } from '../models/types';
export declare function getAccountHolderAddress(account?: AccountInformation): Address;
export declare function getContactBankAddress(account?: AccountInformation): Address;
