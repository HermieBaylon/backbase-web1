import { PaymentHooks } from '../../configs/models/form-item-names.model';
export function triggerPaymentHook(hooks, hookName, callback, formGroup) {
    const hook = hooks && hooks[hookName];
    const doneFn = callback;
    if (typeof hook === 'function') {
        if (hookName === PaymentHooks.onSave ||
            hookName === PaymentHooks.onSubmit) {
            hook({
                doneFn,
                form: formGroup, // The entire payment form
            });
        }
        else {
            hook();
        }
    }
}
//# sourceMappingURL=hooks-helpers.js.map