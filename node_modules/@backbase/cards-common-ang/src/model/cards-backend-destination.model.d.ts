import { Observable } from 'rxjs';
import { Destination } from './travel-notice.model';
/**
 * This interface is used to allow customize the countries/regions service source data
 */
export interface CardsBackendDestinations {
    getDestinations: () => Observable<Destination[]>;
}
