import { PaymentCardDeliveryStepStatus } from './payment-card-delivery-status.model';
/**
 * Mapping from http response values for `delivery.transitStep.status` field to internal
 * `PaymentCardDeliveryStepStatus` value.
 */
export interface PaymentCardDeliveryStepStatusMapping {
    [deliveryStepStatus: string]: PaymentCardDeliveryStepStatus;
}
