import { PaymentCardReplacementStatus } from './payment-card.model';
/**
 * Mapping from http response values for `replacementStatus` field to internal
 * `PaymentCardReplacementStatus` value.
 */
export interface PaymentCardReplacementStatusMapping {
    [replacementStatus: string]: PaymentCardReplacementStatus;
}
