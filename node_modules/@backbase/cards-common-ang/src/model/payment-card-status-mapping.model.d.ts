import { PaymentCardStatus } from './payment-card.model';
/**
 * Mapping from http response values for `status` field to internal `PaymentCardStatus` value.
 */
export interface PaymentCardStatusMapping {
    [status: string]: PaymentCardStatus;
}
