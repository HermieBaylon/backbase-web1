import { PaymentCardLockStatus } from './payment-card.model';
/**
 * Mapping from http response values for `lockStatus` field to internal `PaymentCardLockStatus`
 * value.
 */
export interface PaymentCardLockStatusMapping {
    [lockStatus: string]: PaymentCardLockStatus;
}
