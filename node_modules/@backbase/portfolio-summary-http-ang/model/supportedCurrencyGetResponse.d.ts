import { SupportedCurrency } from './supportedCurrency';
export interface SupportedCurrencyGetResponse {
    ["supportedCurrency"]: Array<SupportedCurrency>;
    ["selectedCurrency"]?: SupportedCurrency;
}
