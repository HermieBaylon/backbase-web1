import { ProductSummaryItem } from '@backbase/product-summary-common-ang';
export interface AccountItem {
    id: string;
    name: string;
    balance?: number;
    number?: string;
    currency?: string;
    favorite?: boolean;
    bankBranchCode?: string;
}
export declare namespace AccountsNamespace {
    function formatHttpresponse(accountList: ProductSummaryItem[]): AccountItem[];
}
