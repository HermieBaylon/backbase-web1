export declare class ParseSuccess<T> {
    readonly result: T;
    static create<T>(result: T): ParseSuccess<T>;
    constructor(result: T);
}
export declare class ParseFailure<R> {
    readonly reason: R;
    static create<R>(reason: R): ParseFailure<R>;
    constructor(reason: R);
}
export declare type ParseResult<T, R> = ParseSuccess<T> | ParseFailure<R>;
