export declare type BatchStatus = 'OPEN' | 'CLOSED' | 'DRAFT' | 'INVALID' | 'VALID' | 'ENTERED' | 'REJECTED' | 'CANCELLED' | 'READY' | 'ACKNOWLEDGED' | 'DOWNLOADING' | 'ACCEPTED' | 'PROCESSED';
export declare const BatchStatus: {
    OPEN: BatchStatus;
    CLOSED: BatchStatus;
    DRAFT: BatchStatus;
    INVALID: BatchStatus;
    VALID: BatchStatus;
    ENTERED: BatchStatus;
    REJECTED: BatchStatus;
    CANCELLED: BatchStatus;
    READY: BatchStatus;
    ACKNOWLEDGED: BatchStatus;
    DOWNLOADING: BatchStatus;
    ACCEPTED: BatchStatus;
    PROCESSED: BatchStatus;
};
