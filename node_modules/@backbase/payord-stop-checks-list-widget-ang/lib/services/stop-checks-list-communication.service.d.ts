import { Observable, Subject } from 'rxjs';
export declare abstract class StopChecksListCommunicationService {
    readonly refreshListSub: Subject<void>;
    readonly refreshList$: Observable<void>;
}
