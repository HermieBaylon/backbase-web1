import { BehaviorSubject, Observable, Subject } from 'rxjs';
import { StopChecksListPropertiesService } from './stop-checks-list-properties.service';
import { StopChecksHttpService } from '@backbase/data-ang/stop-checks';
import { HttpErrorResponse } from '@angular/common/http';
import { LoadMoreMethod, StopCheck } from '@backbase/payment-orders-ang';
import * as i0 from "@angular/core";
export declare class StopChecksListService {
    private readonly stopChecksDataHttpService;
    private readonly preferences;
    private readonly currentPage$;
    private readonly params$;
    private readonly stopChecksData$;
    private readonly totalItems$;
    readonly error$: Subject<HttpErrorResponse>;
    readonly loading$: BehaviorSubject<boolean>;
    readonly getData$: Observable<StopCheck[]>;
    readonly paginationConfig$: Observable<{
        currentPage: number;
        totalItems: number | undefined;
        maxNavPages: number;
        size: number;
        paginationType: LoadMoreMethod;
    }>;
    constructor(stopChecksDataHttpService: StopChecksHttpService, preferences: StopChecksListPropertiesService);
    private getStopChecks;
    loadMore(page: number): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<StopChecksListService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<StopChecksListService>;
}
