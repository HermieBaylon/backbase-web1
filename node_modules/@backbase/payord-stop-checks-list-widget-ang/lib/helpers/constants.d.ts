import { LoadMoreMethod } from '@backbase/payment-orders-ang';
export declare const defaultChecksParams: PaginationConfig;
export interface PaginationConfig {
    size: number;
    maxNavPages: number;
    paginationType: LoadMoreMethod;
    totalItems?: number;
    currentPage: number;
}
