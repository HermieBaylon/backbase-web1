import { Device } from '@backbase/data-ang/device';
export { Device, DeviceStatus } from '@backbase/data-ang/device';
export declare enum UpdateAction {
    Suspend = 0,
    Restore = 1,
    Remove = 2,
    Save = 3
}
export interface ModifiedDevice extends Device {
    oldFriendlyName?: string;
}
