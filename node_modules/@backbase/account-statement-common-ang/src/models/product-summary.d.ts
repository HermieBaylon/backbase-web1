import { Observable } from 'rxjs';
export interface AccountItem {
    amount: number;
    currency: string;
    id: string;
    identifier: string;
    name: string;
}
export declare type Accounts = Array<AccountItem>;
export interface StateAccounts {
    data: Observable<Accounts | null> | undefined;
    error?: boolean;
}
