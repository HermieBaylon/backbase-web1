import { SortDirection, OrderByField } from '@backbase/data-ang/arrangements';
export interface AccountsGetRequestParams {
    from?: number;
    cursor?: string;
    size?: number;
    orderBy?: OrderByField[];
    direction?: SortDirection;
}
