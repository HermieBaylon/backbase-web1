import { combineLatest } from 'rxjs';
import { map } from 'rxjs/operators';
export const getPropertyValue = (input, prop) => combineLatest([input, prop]).pipe(map(([inputVal, propVal]) => (inputVal !== undefined ? inputVal : propVal)));
//# sourceMappingURL=item-model-property.js.map