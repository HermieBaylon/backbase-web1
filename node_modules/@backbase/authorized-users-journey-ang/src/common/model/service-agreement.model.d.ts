import { HttpResponse } from '@angular/common/http';
import { ServiceAgreementStatePut, Status } from '@backbase/data-ang/accesscontrol';
export interface ServiceAgreementRequest {
    validUntilDate: string;
    validUntilTime: string;
    status: string;
    additions: {
        [key: string]: any;
    };
}
export declare namespace ServiceAgreementRequest {
    function toHttpRequest(state: Status): ServiceAgreementStatePut;
    function fromHttpResponse(response: HttpResponse<any>): any;
}
