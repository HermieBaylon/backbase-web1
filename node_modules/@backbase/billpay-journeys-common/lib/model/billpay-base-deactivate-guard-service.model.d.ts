import { ActivatedRouteSnapshot, CanDeactivate, Router, RouterStateSnapshot } from '@angular/router';
import { Location } from '@angular/common';
import { Observable } from 'rxjs';
import { BillpayDiscardChangesFormComponent } from './billpay-discard-changes-form-component.model';
export declare abstract class BillpayBaseDeactivateGuardService implements CanDeactivate<BillpayDiscardChangesFormComponent> {
    protected readonly router: Router;
    protected readonly location: Location;
    protected abstract readonly allowedPreviousRoutes: RegExp[];
    protected abstract readonly allowedNextRoutes: RegExp[];
    /** @internal */
    constructor(router: Router, location: Location);
    canDeactivate(component: BillpayDiscardChangesFormComponent, currentRoute: ActivatedRouteSnapshot, currentState: RouterStateSnapshot, nextState: RouterStateSnapshot): Observable<boolean> | boolean;
}
