import { ConversationThread } from '@backbase/messages-v5-http-ang';
export interface MessagesConversationThreadWidgetContext {
    conversation: ConversationThread;
    maxAttachmentSize: string;
    replyMessageMaxLength: string;
    mailboxType: string;
}
