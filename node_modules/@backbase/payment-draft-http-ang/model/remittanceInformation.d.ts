export interface RemittanceInformation {
    ["type"]: RemittanceInformation.TypeEnum;
    ["content"]: string;
    ["additions"]?: {
        [key: string]: string;
    };
}
export declare namespace RemittanceInformation {
    type TypeEnum = 'STRUCTURED' | 'UNSTRUCTURED';
    const TypeEnum: {
        STRUCTURED: TypeEnum;
        UNSTRUCTURED: TypeEnum;
    };
}
