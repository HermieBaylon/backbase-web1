/**
 * Check for loan overdue
 *
 * @param isOverdue
 * @param inArrearsAmount
 * @param inArrearsDate
 */
export function isLoanOverdue({ isOverdue, inArrearsAmount, inArrearsDate, overduePaymentsCount, }) {
    return Boolean(isOverdue || inArrearsAmount || inArrearsDate || overduePaymentsCount);
}
/**
 * Return the alias for loan account
 *
 * @param alias
 * @param name
 */
export function getLoanAlias({ alias, name }) {
    return alias || alias === '' ? alias : name;
}
//# sourceMappingURL=loan.util.js.map