import { PresentationLoan } from '@backbase/data-ang/loans';
export declare type LoanViewType = LoanDefaultViewType | string;
export declare enum LoanDefaultViewType {
    LineOfCredit = "LineOfCredit",
    Mortgage = "Mortgage",
    TermLoan = "TermLoan"
}
export interface Loan extends PresentationLoan {
    viewType: LoanViewType;
    loanTypeAlias: string;
}
