import { PresentationLoan } from '@backbase/data-ang/loans';
/**
 * Check for loan overdue
 *
 * @param isOverdue
 * @param inArrearsAmount
 * @param inArrearsDate
 */
export declare function isLoanOverdue<T extends PresentationLoan = PresentationLoan>({ isOverdue, inArrearsAmount, inArrearsDate, overduePaymentsCount, }: T): boolean;
/**
 * Return the alias for loan account
 *
 * @param alias
 * @param name
 */
export declare function getLoanAlias<T extends PresentationLoan = PresentationLoan>({ alias, name }: T): string | undefined;
