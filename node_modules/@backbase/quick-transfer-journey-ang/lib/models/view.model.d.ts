/** Enum representing different view states */
export declare enum ViewState {
    FORM = "FORM",
    FROM = "FROM",
    PAYEE = "PAYEE",
    COMPLETE = "COMPLETE",
    REVIEW = "REVIEW"
}
export declare enum Location {
    FROM = "FROM",
    TO = "TO"
}
