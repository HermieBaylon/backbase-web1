import { HttpResponseBase } from '@angular/common/http';
export declare function hasStatusWithHeaderValue(response: HttpResponseBase, status: number, headerKey: string, headerValue: string): boolean;
