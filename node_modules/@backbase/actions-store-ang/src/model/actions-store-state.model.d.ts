import { ActionRecipeSpecification } from '@backbase/data-ang/actions';
import { ActionRecipeItem } from '@backbase/actions-common-ang';
export interface SpecificationsState {
    data: ActionRecipeSpecification[];
    loading: boolean;
    error: boolean;
}
export interface ActionRecipesState {
    data: ActionRecipeItem[];
    loading: boolean;
    error?: Error;
    specificationIds?: Set<string>;
}
export interface ActionsState {
    specifications: SpecificationsState;
    actionRecipes: ActionRecipesState;
}
