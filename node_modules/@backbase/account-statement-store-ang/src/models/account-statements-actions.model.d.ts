import { AccountStatement } from '@backbase/data-ang/account-statements';
import { PaginationParams } from './pagination-params.model';
import { AccountStatementsFilterParams, SortingParams, WidgetPreferences } from '../account-statements/reducers';
export interface LoadAccountStatementsBusinessPayload {
    accountIds?: string[];
    filterParams?: AccountStatementsFilterParams;
    paginationParams?: PaginationParams;
    sortingParams?: SortingParams;
    append?: boolean;
}
export interface LoadAccountStatementsBusinessSuccessPayload extends LoadAccountStatementsBusinessPayload {
    accountStatements: AccountStatement[];
    totalCount: number;
}
export interface UpdateWidgetPreferencesPayload {
    widgetPreferences: Partial<WidgetPreferences>;
}
