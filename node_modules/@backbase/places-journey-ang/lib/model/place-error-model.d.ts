import { HttpErrorResponse } from '@angular/common/http';
export interface PlacesError {
    message: string;
    key?: string;
    context?: object;
}
export declare const parseError: (error: HttpErrorResponse) => PlacesError;
