import { ElementRef, Renderer2, Injector, ComponentFactoryResolver, ViewContainerRef, NgZone, TemplateRef, ChangeDetectorRef, ApplicationRef } from '@angular/core';
import { NgbTooltip, NgbTooltipConfig } from '@ng-bootstrap/ng-bootstrap';
import * as i0 from "@angular/core";
/**
 * @name TooltipDirective
 *
 * @description
 * Directive that displays a tooltip.
 */
export declare class TooltipDirective extends NgbTooltip {
    el: ElementRef;
    /**
     * Content to be displayed as tooltip. If falsy, the tooltip won't open.
     */
    set bbTooltip(value: string | TemplateRef<any> | null | undefined);
    /**
     * Specifies the events that should trigger the tooltip (space separated strings). Defaults to 'click'.
     *
     * This component extends `NgbTooltip`.
     * All available properties of `NgbTooltip` can be used with this component as well.
     * More information about it and more examples can be found in
     * [NgBootstrap documentation](https://ng-bootstrap.github.io/#/components/tooltip/api).
     */
    triggers: string;
    constructor(el: ElementRef, renderer2: Renderer2, injector: Injector, componentFactoryResolver: ComponentFactoryResolver, viewContainerRef: ViewContainerRef, config: NgbTooltipConfig, _ngZone: NgZone, _document: any, ref: ChangeDetectorRef, appref: ApplicationRef);
    /**
     * Event handler to set aria-expanded on opening the tooltip
     */
    open(context?: any): void;
    /**
     * Event handler to set aria-expanded on closing the tooltip
     */
    close(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<TooltipDirective, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<TooltipDirective, "[bbTooltip]", never, { "bbTooltip": "bbTooltip"; "triggers": "triggers"; }, {}, never>;
}
