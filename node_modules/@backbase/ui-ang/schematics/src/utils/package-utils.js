"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readFileOrError = exports.readFile = exports.getWorkspace = void 0;
const workspace_1 = require("@schematics/angular/utility/workspace");
function getWorkspace(host) {
    return __awaiter(this, void 0, void 0, function* () {
        const ws = yield workspace_1.getWorkspace(host);
        return ws;
    });
}
exports.getWorkspace = getWorkspace;
function readFile(host, path) {
    if (!host.exists(path)) {
        return undefined;
    }
    const source = host.read(path);
    if (!source) {
        return undefined;
    }
    return source.toString('utf-8');
}
exports.readFile = readFile;
function readFileOrError(host, path) {
    const result = readFile(host, path);
    if (result === undefined) {
        console.error(`No file found on ${path}. Please check the path`);
    }
    return result;
}
exports.readFileOrError = readFileOrError;
//# sourceMappingURL=package-utils.js.map