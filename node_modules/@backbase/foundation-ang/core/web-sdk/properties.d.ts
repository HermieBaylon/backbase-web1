import { Property, Properties } from '@backbase/foundation-ang/web-sdk';
export declare function equal(a: Property, b: Property): boolean;
/**
 * Allow the processing of Properties as an array of pairs.
 *
 * @param process The processing to apply to the property pairs; mapping, filtering, etc.
 * @param properties
 */
export declare const propertiesAsPairs: (process: (pairs: Array<[string, Property]>) => Array<[string, Property]>, properties: Properties) => Properties;
/**
 * Returns a subset of the properties that start
 * with a given prefix, and also removes the prefix from the name
 * of the property.
 *
 * @param prefix
 * @param properties
 */
export declare const propertiesByPrefix: (prefix: string, properties: Properties) => Properties;
