import { FlowsService } from './flows.service';
import { ӨItemNavigationService } from '../item-navigation.service';
import { ItemModel } from '../../models/item-model';
export declare class ItemTransitionService {
    private readonly flows;
    private readonly navigationService;
    constructor(flows: FlowsService, navigationService: ӨItemNavigationService);
    transition(fromItem: ItemModel, fromOutput: string, toItem: ItemModel, toInput: string | undefined, ancestors: Array<ItemModel>, payload: any): Promise<boolean>;
    private mapFlows;
}
