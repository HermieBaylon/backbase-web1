export interface Edge<S> {
    value: S;
}
/**
 * @deprecated
 */
export declare class Graph<T, S> {
    vertexes: Set<T>;
    edges: Map<T, Map<T, Edge<S>>>;
    add(vertex: T): void;
    connect(from: T, to: T, value: S): void;
    getEdge(from: T, to: T): S | undefined;
}
