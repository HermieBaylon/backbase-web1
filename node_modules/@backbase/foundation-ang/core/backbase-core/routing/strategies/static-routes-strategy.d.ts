import { Routes } from '@angular/router';
import { RoutingStrategy, WidgetRoute } from './routing-decorators';
export declare class StaticRoutesStrategy implements RoutingStrategy {
    private readonly routes;
    constructor(routes?: Array<WidgetRoute>);
    getRoutes(): Routes;
}
