import { Tree, Rule } from '@angular-devkit/schematics';
import { Change } from './ng-utils';
export declare function insert(host: Tree, modulePath: string, changes: Array<Change>): void;
export interface TextChangeOperators {
    insert(pos: number, toAdd: string): void;
    replace(pos: number, oldText: string, newText: string): void;
    remove(pos: number, toRemove: string): void;
}
export declare const astChangeFile: <Ast>(parser: (content: string) => Ast | undefined) => (filePath: string, changes: (ast: Ast, operations: TextChangeOperators) => void) => Rule;
