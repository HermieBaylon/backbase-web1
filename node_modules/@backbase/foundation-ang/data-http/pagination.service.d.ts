import { HttpParams } from '@angular/common/http';
export interface PageParams {
    from: number;
    size: number;
}
export interface CursorParams {
    cursor: string;
    size?: number;
}
export declare type PaginationParams = PageParams | CursorParams;
export declare namespace PaginationParams {
    function equal(a: PaginationParams, b: PaginationParams): boolean;
    function isPageParams(params: PaginationParams): params is PageParams;
    function isCursorParams(params: PaginationParams): params is CursorParams;
    function toHttpParams(params: PaginationParams): HttpParams;
}
