import { Route } from '@angular/router';
export interface DataTypedRoute<T = LoansRouteData> extends Route {
    data: T;
    path: string;
}
export interface LoansRouteData {
    /**
     * Function returns title for the route tab
     */
    titleFn: () => string;
}
