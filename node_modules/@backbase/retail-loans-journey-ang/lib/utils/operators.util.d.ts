import { ObservedValueOf, OperatorFunction, Subscribable } from 'rxjs';
/**
 * catchError with error logging operator
 *
 * @param fn - callback function
 */
export declare function catchErrorWithLog<T, O extends Subscribable<any>>(fn: (error: any) => O): OperatorFunction<T, T | ObservedValueOf<O>>;
