export declare const DOCUMENT_LOAD_DELAY_MS = 400;
import { Observable } from 'rxjs';
export declare type Styles = {
    [key in string]: string;
};
export declare enum DocumentViewerType {
    JPEG = "image/jpeg",
    JPG = "image/jpg",
    PNG = "image/png",
    WEBP = "image/webp",
    GIF = "image/gif",
    BMP = "image/bmp"
}
export interface DocumentRefSrc {
    filename: string;
    filetype: DocumentViewerType;
    src: Observable<string>;
}
export interface DocumentViewerOptions {
    container?: {
        styles?: Styles;
    };
    scale?: number;
}
export interface NavigationState {
    currIdx: number;
    total: number;
}
export declare const isDocumentViewerType: (type: string) => type is DocumentViewerType;
export declare const downloadFile: (url: string, fileName: string) => void;
export declare enum KEY_CODES {
    UP = "ArrowUp",
    DOWN = "ArrowDown",
    ENTER = "Enter",
    SPACE = " ",
    TAB = "Tab",
    BACKSPACE = "Backspace",
    DELETE = "Delete",
    ESCAPE = "Escape",
    X = "x",
    V = "v",
    LEFT = "ArrowLeft",
    RIGHT = "ArrowRight"
}
export declare function getKeyCode(event: KeyboardEvent): KEY_CODES;
