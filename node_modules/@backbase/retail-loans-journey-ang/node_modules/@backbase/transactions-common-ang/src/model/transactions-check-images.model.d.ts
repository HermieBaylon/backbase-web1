import { HttpResponse } from '@angular/common/http';
import { TransactionCheckImagesResponse } from '@backbase/data-ang/transactions';
export declare enum TransactionsCheckImageType {
    PNG = "image/png",
    JPEG = "image/jpeg"
}
export declare enum TransactionsCheckImageSide {
    FRONT = "FRONT",
    BACK = "BACK"
}
export interface TransactionsCheckImage {
    side: TransactionsCheckImageSide;
    mimeType: TransactionsCheckImageType;
    content: string;
}
export interface TransactionCheckImageResponse {
    images: TransactionsCheckImage[];
}
export declare namespace TransactionsCheckImages {
    function fromHttpResponse(response: HttpResponse<TransactionCheckImagesResponse>): TransactionCheckImageResponse;
}
