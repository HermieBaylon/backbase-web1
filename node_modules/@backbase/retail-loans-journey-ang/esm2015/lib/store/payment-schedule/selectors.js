import { createFeatureSelector, createSelector } from '@ngrx/store';
import { paymentScheduleStoreKey } from './reducer';
const createPaymentScheduleSelector = (projector) => 
// eslint-disable-next-line @typescript-eslint/ban-types
createSelector(createFeatureSelector(paymentScheduleStoreKey), projector);
export const selectAmortizedPayments = createPaymentScheduleSelector(({ amortizedPayments }) => amortizedPayments);
export const selectAmortizedPaymentsCallState = createPaymentScheduleSelector(({ amortizedPaymentsCallState }) => amortizedPaymentsCallState);
export const selectAmortizedPaymentsTotalCount = createPaymentScheduleSelector(({ amortizedPaymentsTotalCount }) => amortizedPaymentsTotalCount);
export const selectAmortizedPaymentsCurrentPage = createPaymentScheduleSelector(({ amortizedPaymentsCurrenPage }) => amortizedPaymentsCurrenPage);
export const selectAmortizedPayment = createPaymentScheduleSelector(({ amortizedPayment }) => amortizedPayment);
export const selectAmortizedPaymentCallState = createPaymentScheduleSelector(({ amortizedPaymentCallState }) => amortizedPaymentCallState);
//# sourceMappingURL=selectors.js.map