import { OnInit } from '@angular/core';
import { ContactsService } from '../../../services/contacts.service';
import { ContactItem } from '@backbase/contact-common-ang';
import { AccountCreateType, AccountTypeModelKey } from '../../../models/types';
import * as i0 from "@angular/core";
/**
 * Displays an item in the  list of all contacts
 *
 * @usageNotes
 *
 * ### Display a single item in the list of types of contact.
 *
 * ```html
 * <bb-contact-list-item
 *     [contactItem]="item"
 *     [showAccountNumber]="true"
 *     [showAlias]="showAlias"
 *  ></bb-contact-list-item>
 * ```
 * @ngModule ContactManagerWidgetModule
 */
export declare class ContactsListItemComponent implements OnInit {
    service: ContactsService;
    /**
     * Contact item that contains the account information of the contact
     */
    contactItem: ContactItem;
    /**
     * Wether to show contact account number.
     * Default is false.
     */
    showAccountNumber: boolean;
    /**
     * Wether to show contact alias.
     * Default is false.
     */
    showAlias: boolean;
    readonly accountTypeKeyRef: typeof AccountTypeModelKey;
    readonly createAccountType: typeof AccountCreateType;
    selectedAccountType: string;
    constructor(service: ContactsService);
    /**
     * OnInit - lifecycle hook.
     */
    ngOnInit(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<ContactsListItemComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<ContactsListItemComponent, "bb-contact-list-item", never, { "contactItem": "contactItem"; "showAccountNumber": "showAccountNumber"; "showAlias": "showAlias"; }, {}, never, never>;
}
