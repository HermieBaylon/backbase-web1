import { OnChanges } from '@angular/core';
import { DeprecationsService } from '@backbase/foundation-ang/future';
import { ScaleBand, ScaleLinear } from 'd3-scale';
import { ChartData, ChartDataLabels, Dims } from '../../model/turnovers';
import * as i0 from "@angular/core";
export declare class BbGridPanelSeriesComponent implements OnChanges {
    private deprecationService;
    gridPanels: ChartDataLabels[];
    /**
     * Array of Chart data objects needed for the chart to be rendered.
     */
    data: ChartData[];
    /**
     * Dimensions of the chart.
     */
    dims: Dims;
    /**
     * Horizontal scale of the chart.
     */
    xScale?: ScaleBand<string>;
    /**
     * Vertical scale of the chart.
     */
    yScale?: ScaleLinear<number, number>;
    _orient?: string;
    set orient(val: string);
    get orient(): string;
    ngOnChanges(): void;
    update(): void;
    private getChartDataLabels;
    getGridPanels(): any[];
    constructor(deprecationService: DeprecationsService);
    static ɵfac: i0.ɵɵFactoryDeclaration<BbGridPanelSeriesComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<BbGridPanelSeriesComponent, "g[bb-charts-grid-panel-series]", never, { "data": "data"; "dims": "dims"; "xScale": "xScale"; "yScale": "yScale"; }, {}, never, never>;
}
